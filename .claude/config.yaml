coding:
  default_model: sonnet          # For implement/review
  explore_model: haiku           # Cheaper for exploration  
  ticket_prefix: OCGO            # Conduit tickets
  auto_commit: false             # Manual commits for now
  run_tests: true                # Always run tests